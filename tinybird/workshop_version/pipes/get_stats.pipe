TOKEN "get_stats_endpoint_read_3134" READ

NODE endpoint
SQL >

    WITH player_count AS (
          SELECT COUNT(DISTINCT player_id) AS players
          FROM user_stats_mv
          WHERE event == 'workshop_2024_06_11'
        ),
    game_count AS (
      SELECT COUNT(*) AS games
      FROM flappy_tinybird_workshop.events_api
      WHERE type == 'game_over'
      AND event == 'workshop_2024_06_11'
    )
    SELECT players, games
    FROM player_count, game_count


